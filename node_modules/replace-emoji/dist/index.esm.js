import _typeof from '@babel/runtime/helpers/esm/typeof';

var EmojiRegex = /([\u2700-\u27BF]|[\uE000-\uF8FF]|\uD83C[\uDC00-\uDFFF]|\uD83D[\uDC00-\uDFFF]|[\u2011-\u26FF]|\uD83E[\uDD10-\uDDFF])/g;
/**
 *
 * @param s The string or strings to be replaced
 * @param r The content to be replaced
 * @param reg Customize RegExp
 * @returns The replaced result
 */

var replaceEmoji = function replaceEmoji(s) {
  var r = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';
  var reg = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : EmojiRegex;

  if (typeof s === 'string') {
    return s.replace(reg, r);
  }

  if (_typeof(s) === 'object') {
    var a = [];
    s.forEach(function (i) {
      if (typeof i === 'string') {
        a.push(i.replace(reg, r));
      } else {
        a.push(i);
      }
    });
    return a;
  }

  return '';
};

export default replaceEmoji;
export { replaceEmoji };
